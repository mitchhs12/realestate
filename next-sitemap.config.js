/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://vivaideal.com", // Your domain
  generateRobotsTxt: true, // (optional) Generates robots.txt file
  exclude: ["/private/*"], // (optional) Exclude private pages
};
